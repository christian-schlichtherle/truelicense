<?xml version='1.0'?>
<!--
  ~ Copyright (C) 2005 - 2021 Schlichtherle IT Services.
  ~ All rights reserved. Use is subject to license terms.
  -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>global.namespace.parent-pom</groupId>
        <artifactId>parent-pom</artifactId>
        <version>16</version>
    </parent>

    <groupId>global.namespace.truelicense</groupId>
    <artifactId>truelicense</artifactId>
    <version>4.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>TrueLicense</name>
    <description>
        TrueLicense is an open source engine for license management on the Java Virtual Machine.
        Due to its functional and modular design TrueLicense scales from simple to complex licensing schemas.
        TrueLicense features various interfaces for managing free trial periods, subscriptions, multiple editions,
        internationalization, privacy protection and more.
    </description>
    <url>http://truelicense.net/</url>
    <inceptionYear>2005</inceptionYear>
    <organization>
        <name>Schlichtherle IT Services</name>
        <url>https://schlichtherle.de</url>
    </organization>
    <issueManagement>
        <system>Github</system>
        <url>https://github.com/christian-schlichtherle/truelicense/issues</url>
    </issueManagement>
    <scm>
        <connection>scm:git:https://github.com/christian-schlichtherle/truelicense.git</connection>
        <developerConnection>scm:git:https://github.com/christian-schlichtherle/truelicense.git</developerConnection>
        <url>https://github.com/christian-schlichtherle/truelicense</url>
        <tag>HEAD</tag>
    </scm>

    <properties>
        <!-- maven-jar-plugin: JPMS automatic module name; overridden per module -->
        <automatic.module.name>global.namespace.truelicense</automatic.module.name>

        <!--
          ~ maven-enforcer-plugin: Maven 3.6.x does not merge a profile-injected <pluginManagement> execution into a
          ~ plugin declaration, which silently disabled constant string obfuscation in the 4.0.1 and 4.0.3 releases.
          ~ The wiring below no longer relies on that merge, but older Maven versions remain untested, so require a
          ~ version known to be good.
          -->
        <maven.enforcer.requireMavenVersion>[3.9.16,)</maven.enforcer.requireMavenVersion>

        <!-- maven-compiler-plugin -->
        <maven.compiler.debug>false</maven.compiler.debug>
        <maven.compiler.optimize>true</maven.compiler.optimize>
        <maven.compiler.showDeprecation>true</maven.compiler.showDeprecation>
        <maven.compiler.showWarnings>true</maven.compiler.showWarnings>

        <!-- maven-javadoc-plugin -->
        <maven.javadoc.failOnError>false</maven.javadoc.failOnError>
        <nodeprecated>false</nodeprecated>

        <!-- truelicense-maven-plugin -->
        <truelicense.obfuscate.intern>false</truelicense.obfuscate.intern>
        <truelicense.obfuscate.scope>all</truelicense.obfuscate.scope>
        <!-- Set to true in a module that legitimately has no constant strings to obfuscate. -->
        <truelicense.obfuscate.verify.skip>false</truelicense.obfuscate.verify.skip>
        <!--
          ~ Set to true in a module that obfuscates only named constants: the computation method then takes the name
          ~ of the field it replaces, so no synthesized "_string#N" or "_clinit@..." name appears and the weaker
          ~ "references ObfuscatedString" check has to stand in. Only safe where the module's own source does not
          ~ reference ObfuscatedString.
          -->
        <truelicense.obfuscate.verify.relaxed>false</truelicense.obfuscate.verify.relaxed>

        <!--
          ~ dependencies
          ~
          ~ byte-buddy, neuron-di and scala are pinned here purely to keep the build working on JDKs newer than 11;
          ~ each one blocks it outright, and each masked the next. They would belong to the parent POM, but are kept
          ~ here so that a TrueLicense release never has to wait on a parent POM release. See CLAUDE.md.
          -->
        <asm.version>9.10.1</asm.version>
        <!-- Overrides the 1.10.20 that the parent POM's Mockito 3.9.0 resolves: it cannot read JDK 21+ class files. -->
        <byte-buddy.version>1.18.11</byte-buddy.version>
        <fun-io.version>2.4.1</fun-io.version>
        <jackson.version>2.22.1</jackson.version>
        <!-- Terminal release of the javax.xml.bind:jaxb-api GA - everything newer is the jakarta.* namespace. -->
        <jaxb-api.version>2.3.1</jaxb-api.version>
        <!-- Last of the JAXB 2.3 line, i.e. the newest runtime that still implements the javax.xml.bind packages. -->
        <jaxb-runtime.version>2.3.9</jaxb-runtime.version>
        <!-- Kept in lockstep with maven.enforcer.requireMavenVersion above. -->
        <maven.version>3.9.16</maven.version>
        <!-- maven-plugin-plugin and maven-plugin-annotations must stay on the same version. -->
        <maven-plugin-tools.version>3.15.2</maven-plugin-tools.version>
        <!-- Also overrides the bootstrap plugin's own 6.4.4 - see the plugin declaration below. -->
        <neuron-di.version>6.7.1</neuron-di.version>
        <plexus-utils.version>4.0.3</plexus-utils.version>
        <!-- Overrides the parent POM's 2.13.5, whose Zinc compiler bridge fails to build on JDK 21+. -->
        <scala.version>2.13.18</scala.version>
    </properties>

    <modules>
        <module>api</module>
        <module>build-tasks</module>
        <module>core</module>
        <module>jax-rs</module>
        <module>jsf</module>
        <module>maven-plugin</module>
        <module>obfuscate</module>
        <module>spi</module>
        <module>swing</module>
        <module>tests</module>
        <module>ui</module>
        <module>v1</module>
        <module>v2-core</module>
        <module>v2-json</module>
        <module>v2-xml</module>
        <module>v4</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-api</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-build-tasks</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-jax-rs</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-jsf</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-maven-plugin</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-obfuscate</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-spi</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-swing</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-ui</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-v1</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-v2-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-v2-json</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-v2-xml</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>truelicense-v4</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!--
              ~ Imported rather than pinned per artifact: since 2.20, jackson-annotations no longer carries the patch
              ~ component of the release train version (2.22 vs 2.22.1), so a single ${jackson.version} property
              ~ cannot express the set. The BOM also aligns jackson-module-jaxb-annotations, which Jersey pulls in.
              -->
            <dependency>
                <groupId>com.fasterxml.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>${jackson.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.glassfish.jersey</groupId>
                <artifactId>jersey-bom</artifactId>
                <!-- Last of the Jersey 2.x line, i.e. the newest JAX-RS 2.1 (javax.ws.rs) implementation. -->
                <version>2.48</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>cglib</groupId>
                <artifactId>cglib</artifactId>
                <version>3.3.0</version>
            </dependency>
            <!-- For use by the TrueLicense Maven Archetype with the V2/XML license key format: -->
            <dependency>
                <groupId>com.sun.activation</groupId>
                <artifactId>javax.activation</artifactId>
                <version>1.2.0</version>
            </dependency>
            <dependency>
                <groupId>commons-beanutils</groupId>
                <artifactId>commons-beanutils</artifactId>
                <!--
                  ~ Was pinned to 1.9.3 because 1.9.4 broke `DisplayTool.capitalize` in velocity-tools-generic 3.0,
                  ~ used by the TrueLicense Build Tool and (transitively) the TrueLicense Maven Plugin. That pin is
                  ~ obsolete: velocity-tools-generic is on 3.1 now, and `capitalize`, `uncapitalize` and `truncate`
                  ~ were verified to return identical results there on 1.9.3, 1.9.4 and 1.11.0. Unpinning also picks
                  ~ up the fix for CVE-2019-10086, which 1.9.3 is subject to.
                  -->
                <version>1.11.0</version>
            </dependency>
            <dependency>
                <groupId>commons-digester</groupId>
                <artifactId>commons-digester</artifactId>
                <version>2.1</version>
            </dependency>
            <dependency>
                <groupId>commons-logging</groupId>
                <artifactId>commons-logging</artifactId>
                <version>1.4.0</version>
            </dependency>
            <dependency>
                <groupId>global.namespace.fun-io</groupId>
                <artifactId>fun-io-api</artifactId>
                <version>${fun-io.version}</version>
            </dependency>
            <dependency>
                <groupId>global.namespace.fun-io</groupId>
                <artifactId>fun-io-bios</artifactId>
                <version>${fun-io.version}</version>
            </dependency>
            <dependency>
                <groupId>global.namespace.fun-io</groupId>
                <artifactId>fun-io-jackson</artifactId>
                <version>${fun-io.version}</version>
            </dependency>
            <dependency>
                <groupId>global.namespace.fun-io</groupId>
                <artifactId>fun-io-jaxb</artifactId>
                <version>${fun-io.version}</version>
            </dependency>
            <dependency>
                <groupId>global.namespace.fun-io</groupId>
                <artifactId>fun-io-scala-api_${scala.compat.version}</artifactId>
                <version>${fun-io.version}</version>
            </dependency>
            <dependency>
                <groupId>global.namespace.neuron-di</groupId>
                <artifactId>neuron-di</artifactId>
                <version>${neuron-di.version}</version>
            </dependency>
            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy</artifactId>
                <version>${byte-buddy.version}</version>
            </dependency>
            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy-agent</artifactId>
                <version>${byte-buddy.version}</version>
            </dependency>
            <dependency>
                <groupId>jakarta.annotation</groupId>
                <artifactId>jakarta.annotation-api</artifactId>
                <version>1.3.5</version>
            </dependency>
            <dependency>
                <groupId>javax</groupId>
                <artifactId>javaee-web-api</artifactId>
                <version>8.0.1</version>
            </dependency>
            <dependency>
                <groupId>javax.servlet</groupId>
                <artifactId>javax.servlet-api</artifactId>
                <version>4.0.1</version>
            </dependency>
            <dependency>
                <groupId>javax.ws.rs</groupId>
                <artifactId>javax.ws.rs-api</artifactId>
                <version>2.1.1</version>
            </dependency>
            <dependency>
                <groupId>javax.xml.bind</groupId>
                <artifactId>jaxb-api</artifactId>
                <version>${jaxb-api.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>3.20.0</version>
            </dependency>
            <dependency>
                <groupId>org.apache.maven</groupId>
                <artifactId>maven-core</artifactId>
                <version>${maven.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.maven</groupId>
                <artifactId>maven-plugin-api</artifactId>
                <version>${maven.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.maven.plugin-tools</groupId>
                <artifactId>maven-plugin-annotations</artifactId>
                <version>${maven-plugin-tools.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>2.4.1</version>
            </dependency>
            <dependency>
                <groupId>org.codehaus.plexus</groupId>
                <artifactId>plexus-utils</artifactId>
                <version>${plexus-utils.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.velocity.tools</groupId>
                <artifactId>velocity-tools-generic</artifactId>
                <version>3.1</version>
            </dependency>
            <dependency>
                <groupId>org.glassfish.jaxb</groupId>
                <artifactId>jaxb-runtime</artifactId>
                <version>${jaxb-runtime.version}</version>
            </dependency>
            <dependency>
                <groupId>org.netbeans</groupId>
                <artifactId>jemmy</artifactId>
                <version>2.2.7.5</version>
            </dependency>
            <dependency>
                <groupId>org.ow2.asm</groupId>
                <artifactId>asm-tree</artifactId>
                <version>${asm.version}</version>
            </dependency>
            <dependency>
                <groupId>org.ow2.asm</groupId>
                <artifactId>asm</artifactId>
                <version>${asm.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>${project.groupId}</groupId>
                    <artifactId>truelicense-maven-plugin</artifactId>
                    <!-- Cannot use ${project.version} while bootstrapping! -->
                    <version>4.0.3</version>
                    <dependencies>
                        <!--
                          ~ Maven 3.9+ no longer exports org.codehaus.plexus.util.* from the core realm to plugins,
                          ~ so the bootstrap plugin needs plexus-utils on its own class realm.
                          -->
                        <dependency>
                            <groupId>org.codehaus.plexus</groupId>
                            <artifactId>plexus-utils</artifactId>
                            <version>${plexus-utils.version}</version>
                        </dependency>
                        <!--
                          ~ The bootstrap plugin ships neuron-di 6.4.4, whose generated proxies use a MethodHandle
                          ~ that fails its exact-type check on JDK 17+ (WrongMethodTypeException on the first
                          ~ Logger.info call, i.e. before the goal does any work at all - it even breaks the root
                          ~ aggregator, where the goal is supposed to no-op). 6.7.1 fixes that, but requires the
                          ~ ASM 9 API, so ASM has to be raised past the plugin's own 7.3.1 as well. Without these
                          ~ three overrides the build cannot run on any JDK newer than 11.
                          ~
                          ~ This only repairs the plugin realm used to build *this* project. Applications running
                          ~ the published 4.0.3 plugin on JDK 17+ hit the same crash and need a plugin release.
                          -->
                        <dependency>
                            <groupId>global.namespace.neuron-di</groupId>
                            <artifactId>neuron-di</artifactId>
                            <version>${neuron-di.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-tree</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                    </dependencies>
                </plugin>

                <!--
                  ~ Plugin versions. The parent POM's values date from 2021 and several no longer work on a modern
                  ~ JDK or Maven, so they are overridden here for the same reason as the dependency versions above:
                  ~ so that a TrueLicense release never has to wait on a parent POM release. Every version below was
                  ~ checked to declare java>=1.8 and maven>=3.6.3 in its plugin descriptor, which the JDK 8 CI
                  ~ compile job and the enforced Maven 3.9.16 both satisfy.
                  ~
                  ~ Deliberately NOT bumped: truelicense-maven-plugin, which must stay on the last released version
                  ~ while bootstrapping (see the declaration above).
                  -->
                <plugin>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>3.5.0</version>
                </plugin>
                <plugin>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>3.1.4</version>
                </plugin>
                <plugin>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>3.6.3</version>
                </plugin>
                <plugin>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>3.2.8</version>
                </plugin>
                <plugin>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>3.1.4</version>
                </plugin>
                <plugin>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>3.12.0</version>
                </plugin>
                <plugin>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>3.5.0</version>
                </plugin>
                <plugin>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>3.4.0</version>
                </plugin>
                <plugin>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.6</version>
                </plugin>
                <plugin>
                    <groupId>org.scalatest</groupId>
                    <artifactId>scalatest-maven-plugin</artifactId>
                    <version>2.2.0</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>versions-maven-plugin</artifactId>
                    <version>2.21.0</version>
                </plugin>
                <!--
                  ~ These two are the release path, and NOTHING in an ordinary build or in CI executes their goals -
                  ~ `release:perform` is invoked by hand, and nexus-staging only binds to `deploy`, which neither
                  ~ `install` nor `verify` reaches. So unlike every other version here, these two are NOT covered by
                  ~ the JDK 8-25 matrix; the first real exercise is the next release. maven-release-plugin in
                  ~ particular is a major-version jump from the parent POM's 2.5.3.
                  -->
                <plugin>
                    <artifactId>maven-release-plugin</artifactId>
                    <version>3.3.1</version>
                </plugin>
                <plugin>
                    <groupId>org.sonatype.plugins</groupId>
                    <artifactId>nexus-staging-maven-plugin</artifactId>
                    <version>1.7.0</version>
                </plugin>
                <plugin>
                    <artifactId>maven-antrun-plugin</artifactId>
                    <version>3.2.0</version>
                </plugin>
                <!--
                  ~ Overrides the parent POM's 2.22.2, which cannot run the Jersey integration tests any more.
                  ~ jersey-test-framework-provider-inmemory puts JUnit Jupiter on the test classpath, so Failsafe
                  ~ switches from its JUnit 4 provider to the JUnit Platform provider - and 2.22.2 ships
                  ~ junit-platform-launcher 1.3.2, which is too old for the junit-platform-engine that Jersey drags
                  ~ in. The result is not an error but "Tests run: 0" and a green build, so the four
                  ~ *ConsumerLicenseManagementServiceJerseyIT suites silently stop running. Those suites now run
                  ~ on the JUnit Platform, which 3.x supports; see also the verify-integration-tests-ran check in
                  ~ tests/pom.xml, which turns a recurrence into a build failure.
                  -->
                <plugin>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>3.5.6</version>
                </plugin>
                <plugin>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.15.0</version>
                    <configuration>
                        <compilerArgs>
                            <arg>-Xlint:unchecked</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
                <plugin>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>3.5.1</version>
                    <configuration>
                        <archive>
                            <manifestEntries>
                                <Automatic-Module-Name>${automatic.module.name}</Automatic-Module-Name>
                            </manifestEntries>
                        </archive>
                    </configuration>
                    <executions>
                        <execution>
                            <id>default-jar</id>
                            <configuration>
                                <skipIfEmpty>true</skipIfEmpty>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <artifactId>maven-plugin-plugin</artifactId>
                    <version>${maven-plugin-tools.version}</version>
                    <executions>
                        <execution>
                            <id>default-helpmojo</id>
                            <goals>
                                <goal>helpmojo</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>net.alchim31.maven</groupId>
                    <artifactId>scala-maven-plugin</artifactId>
                    <version>4.9.10</version>
                    <configuration>
                        <args>
                            <arg>-deprecation</arg>
                            <arg>-explaintypes</arg>
                            <arg>-feature</arg>
                        </args>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>enable-obfuscate-main-classes</id>
            <activation>
                <file>
                    <exists>src/main</exists>
                </file>
            </activation>
            <build>
                <!--
                  ~ Declared in <plugins>, NOT <pluginManagement>: pluginManagement binds nothing on its own, and
                  ~ Maven 3.6.x silently fails to merge a profile-injected pluginManagement execution into a module's
                  ~ bare <plugin> declaration. That is what shipped 4.0.1 and 4.0.3 without any obfuscation.
                  ~ Declaring it here obfuscates every module with src/main, so modules must NOT repeat it.
                  -->
                <plugins>
                    <plugin>
                        <groupId>${project.groupId}</groupId>
                        <artifactId>truelicense-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>obfuscate-main-classes</id>
                                <goals>
                                    <goal>obfuscate-main-classes</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!--
          ~ Release-only gate. Obfuscation failing is otherwise invisible: the goal simply does not run and the build
          ~ stays green. This is deliberately NOT part of an ordinary `mvn install` - it is what must hold for an
          ~ artifact that gets published. It runs on `release:perform` (parent-pom sets releaseProfiles to this id)
          ~ and in the CI compile job, which activates this profile explicitly.
          ~
          ~ The signal is a method name synthesized by ObfuscateClassesTask - "_clinit@..." from the <clinit> merger,
          ~ or "_string#N" from an obfuscated ldc. Both contain characters that are illegal in Java identifiers, so
          ~ javac cannot produce them and there are no false passes.
          ~
          ~ Merely referencing ObfuscatedString is NOT sufficient evidence: obfuscate, build-tasks, core and
          ~ maven-plugin all reference that class from their own source, so such a check stays green even when they
          ~ are not obfuscated at all. It is only a fallback for modules that legitimately synthesize no method name
          ~ (see truelicense.obfuscate.verify.relaxed).
          -->
        <profile>
            <id>sonatype-oss-release</id>
            <build>
                <plugins>
                    <!--
                      ~ Obfuscation failing is otherwise invisible: the goal simply does not run and the build stays
                      ~ green. The primary signal is a method name synthesized by ObfuscateClassesTask - "_clinit@..."
                      ~ from the <clinit> merger, or "_string#N" from an obfuscated ldc. Both contain characters that
                      ~ are illegal in Java identifiers, so javac cannot produce them and there are no false passes.
                      ~
                      ~ Merely referencing ObfuscatedString is NOT sufficient evidence: obfuscate, build-tasks, core
                      ~ and maven-plugin all reference that class from their own source, so such a check stays green
                      ~ even when they are not obfuscated at all. It is only used as a fallback for the modules that
                      ~ legitimately synthesize no method name (see truelicense.obfuscate.verify.relaxed).
                      -->
                    <plugin>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>verify-obfuscate-main-classes</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <resourcecount property="synthesizedNameCount">
                                            <fileset dir="${project.build.outputDirectory}" erroronmissingdir="false">
                                                <include name="**/*.class"/>
                                                <or>
                                                    <contains text="_clinit@"/>
                                                    <contains text="_string#"/>
                                                </or>
                                            </fileset>
                                        </resourcecount>
                                        <resourcecount property="obfuscatedStringRefCount">
                                            <fileset dir="${project.build.outputDirectory}" erroronmissingdir="false">
                                                <include name="**/*.class"/>
                                                <contains text="truelicense/obfuscate/ObfuscatedString"/>
                                            </fileset>
                                        </resourcecount>
                                        <!-- This profile has no src/main activation, so skip modules with no classes. -->
                                        <resourcecount property="classFileCount">
                                            <fileset dir="${project.build.outputDirectory}" erroronmissingdir="false">
                                                <include name="**/*.class"/>
                                            </fileset>
                                        </resourcecount>
                                        <fail message="Constant string obfuscation produced nothing in ${project.artifactId}: no class in ${project.build.outputDirectory} carries a method name synthesized by ObfuscateClassesTask. The obfuscate-main-classes goal did not run or did not apply. If this module genuinely has no constant strings, set truelicense.obfuscate.verify.skip=true; if it obfuscates only named constants and so synthesizes no method name, set truelicense.obfuscate.verify.relaxed=true.">
                                            <condition>
                                                <and>
                                                    <available file="${project.build.outputDirectory}" type="dir"/>
                                                    <not>
                                                        <equals arg1="${classFileCount}" arg2="0"/>
                                                    </not>
                                                    <not>
                                                        <istrue value="${truelicense.obfuscate.verify.skip}"/>
                                                    </not>
                                                    <equals arg1="${synthesizedNameCount}" arg2="0"/>
                                                    <or>
                                                        <not>
                                                            <istrue value="${truelicense.obfuscate.verify.relaxed}"/>
                                                        </not>
                                                        <equals arg1="${obfuscatedStringRefCount}" arg2="0"/>
                                                    </or>
                                                </and>
                                            </condition>
                                        </fail>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
